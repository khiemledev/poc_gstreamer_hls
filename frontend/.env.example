# Backend API base URL (optional, defaults to window.location.origin + /api)
VITE_API_BASE_URL=http://localhost:8000/api

# HLS stream base URL (optional, defaults to window.location.origin)
VITE_HLS_BASE_URL=http://localhost:8081
